export class Samurai
{
    //all the properties we get from API when its JSON, its small letters. it gets translated small letters. NOT THE VALUE.
    id?: number=0; 
    name?: string=""; 
    description?: string=""; 
    age?: number=0; 

    

}
